server:
  port: 8080
zuul:
  routes:
    user:
      path: /api/users/**
      stripPrefix: false
  add-proxy-headers: true

ribbon:
  ReadTimeout: 15000


logging:
  level:
    org.springframework:  debug
